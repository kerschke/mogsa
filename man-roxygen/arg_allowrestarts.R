#' @param allow.restarts [\code{\link{logical}(1L)}]\cr
#'   Are restarts allowed within the local search procedure? This will only
#'   have an effect, if the local search procedure gets stuck. The default
#'   is \code{TRUE}.
